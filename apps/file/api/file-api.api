syntax = "v1"

type UploadRequest {}

// 定义文件上传的响应
type UploadResponse {
	FileUrl string `json:"file_url"` // 文件访问URL
}

// 定义文件上传接口
service upload-api {
	@handler UploadHandler
	post /wallpaper/file/upload (UploadRequest) returns (UploadResponse)
}

// goctl api go -api file-api.api -dir .
